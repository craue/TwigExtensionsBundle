craue_twig_extensions: ~

framework:
  form: ~
  router:
    resource: ~
  secret: whatever
  templating:
    engines: ['twig']
  test: ~
  translator:
    fallback: en

twig:
  debug: "%kernel.debug%"
  strict_variables: "%kernel.debug%"
