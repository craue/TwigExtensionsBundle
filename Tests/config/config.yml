framework:
  form:
    csrf_protection: false
  router:
    resource: ~
  secret: whatever
  test: ~
  translator:
    fallback: en

twig:
  debug: '%kernel.debug%'
  strict_variables: '%kernel.debug%'
