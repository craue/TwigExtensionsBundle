framework:
  form:
    csrf_protection: false
  secret: whatever
  test: ~
  translator:
    fallback: en

twig:
  debug: '%kernel.debug%'
  strict_variables: '%kernel.debug%'
  exception_controller: ~
