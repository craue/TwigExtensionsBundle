craue_twig_extensions: ~

framework:
  router:
    resource: ~
  secret: whatever
  templating:
    engines: ['twig']
  test: ~
  translator:
    fallback: en

twig:
  debug: %kernel.debug%
  strict_variables: %kernel.debug%
